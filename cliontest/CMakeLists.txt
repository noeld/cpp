cmake_minimum_required(VERSION 3.6)
project(cliontest)

set(CMAKE_CXX_STANDARD 14)
set(CMAKE_PREFIX_PATH "C:/usr/wxWidgets-3.1.2/;C:/usr/boost_1_71_0")

MACRO(CREATE_FINAL_FILE _filename)
    FILE(WRITE ${_filename} "//autogenerated file\n")
    FOREACH (_current_FILE ${ARGN})
        FILE(APPEND ${_filename} "#include \"${_current_FILE}\"\n")
    ENDFOREACH (_current_FILE)
ENDMACRO(CREATE_FINAL_FILE _filename)

# FIND_PACKAGE(Boost)
find_package(Boost COMPONENTS system filesystem REQUIRED)
IF(Boost_FOUND)
    INCLUDE_DIRECTORIES(${Boost_INCLUDE_DIR})
    LINK_DIRECTORIES(${Boost_LIBRARY_DIR})
    ADD_DEFINITIONS("-DHAS_BOOST")
ENDIF()

message(STATUS "Boost Libraries in ${Boost_LIBRARY_DIR}")
message(STATUS "Boost Libraries : ${Boost_LIBRARIES}")
message(STATUS "Boost Includes: ${Boost_INCLUDE_DIR}")

#include(CPack)

set(wxWidgets_CONFIGURATION mswu)
find_package(wxWidgets COMPONENTS core base REQUIRED )
include(${wxWidgets_USE_FILE})
#set(CMAKE_CXX_COMPILER g++-7)
set(CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS_RELEASE} -O3")
#set(CMAKE_BUILD_TYPE Release)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -march=native -mtune=native -ffast-math")
set(SOURCE_FILES main.cpp Endianness.cpp Endianness.h Range2D.h Mandelbrot.cpp Mandelbrot.h Vector.cpp Vector.h Planet.cpp Planet.h Universe.cpp Universe.h CLionTestApp.cpp CLionTestApp.h CLionTestFrame.cpp CLionTestFrame.h PlanetGenerator.cpp PlanetGenerator.h Properties.cpp Properties.h Histogram.h TimeMeasure.cpp TimeMeasure.h)
CREATE_FINAL_FILE(_final.cpp ${SOURCE_FILES})
add_executable(cliontest _final.cpp)
target_link_libraries(${PROJECT_NAME} ${wxWidgets_LIBRARIES})
target_link_libraries(${PROJECT_NAME} ${Boost_LIBRARIES})
